# 云存储文件夹结构说明

## 📁 正确的文件夹结构

在云开发控制台的**云存储**中，应该创建以下文件夹结构：

```
云存储根目录/
├── images/              ← 新建这个文件夹（项目图片）
│   ├── banner/         ← Banner 图片
│   │   ├── leaders1.jpeg
│   │   ├── leaders2.jpeg
│   │   └── ...
│   ├── icon/           ← Icon 图片
│   │   ├── action-icon.png
│   │   ├── generator-icon.png
│   │   └── ...
│   └── tabbar/         ← TabBar 图标（可选，因为 TabBar 通常用本地路径）
│       ├── home.png
│       ├── home-active.png
│       └── ...
│
├── weda-richtext-upload/  ← 微信自动创建（不要使用）
└── weda-uploader/         ← 微信自动创建（不要使用）
```

## ✅ 操作步骤

### 1. 创建文件夹

在云开发控制台 → 存储 → 文件管理：

1. 点击"新建文件夹"
2. 输入文件夹名称：`images`
3. 在 `images` 文件夹下创建子文件夹：
   - `banner`
   - `icon`
   - `tabbar`（可选）

### 2. 上传图片

按照以下分类上传：

#### Banner 图片
- 上传到：`images/banner/`
- 文件：
  - `leaders1.jpeg`
  - `leaders2.jpeg`
  - `leaders3.jpeg`
  - `leaders4.png`
  - `leaders5.jpeg`
  - `leaders6.jpeg`

#### Icon 图片
- 上传到：`images/icon/`
- 文件：
  - `action-icon.png`
  - `generator-icon.png`
  - `learn-more-arrow.png`
  - `more-arrow.png`

#### TabBar 图标（可选）
- 上传到：`images/tabbar/`
- 文件：
  - `home.png`
  - `home-active.png`
  - `generator.png`
  - `generator-active.png`
  - `action.png`
  - `action-active.png`

**注意**：TabBar 图标通常使用本地路径，上传到云存储是可选的。

### 3. 保存配置到数据库

上传完成后，需要将图片路径保存到 `image_config` 数据库集合。

可以使用工具脚本自动完成，或手动调用云函数：

```javascript
// 示例：保存一个 Banner 图片配置
wx.cloud.callFunction({
  name: 'uploadImage',
  data: {
    cloudPath: 'cloud://xxx.xxx/images/banner/leaders1.jpeg',
    category: 'banner',
    page: 'leaders',
    order: 1
  }
})
```

## ⚠️ 重要提示

### 不要使用自动创建的文件夹

- ❌ **不要**使用 `weda-richtext-upload` 文件夹
- ❌ **不要**使用 `weda-uploader` 文件夹

这些文件夹是微信自动创建的，用于特定功能（可能是 CMS 或其他组件），使用它们可能会导致：
- 路径混乱
- 文件被自动清理
- 与其他功能冲突

### 使用项目专用文件夹

- ✅ **使用** `images/` 文件夹
- ✅ 在 `images/` 下创建子文件夹分类管理
- ✅ 保持文件夹结构清晰

## 📝 完整路径示例

上传后的完整云存储路径格式：

```
cloud://环境ID/images/banner/leaders1.jpeg
cloud://环境ID/images/icon/action-icon.png
cloud://环境ID/images/tabbar/home.png
```

这些路径会被保存到 `image_config` 数据库集合中，然后通过 `getImageConfig` 云函数返回给小程序使用。

## 🔍 验证

上传完成后，可以：

1. **检查文件**：
   - 在云存储中查看 `images/` 文件夹
   - 确认所有图片都已上传

2. **检查配置**：
   - 在数据库中查看 `image_config` 集合
   - 确认每条记录都有正确的 `cloudPath`

3. **测试加载**：
   - 在小程序中调用 `getImageConfig` 云函数
   - 检查返回的图片 URL 是否正确



